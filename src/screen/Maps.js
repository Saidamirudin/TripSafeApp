import React from 'react'
import { ScrollView, Text } from 'react-native'

const Maps = () => {
    return (
        <ScrollView>
            <Text>
                Maps Menu
            </Text>
        </ScrollView>
    )
}

export default Maps