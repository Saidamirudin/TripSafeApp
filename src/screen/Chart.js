import React from 'react'
import { ScrollView, Text } from 'react-native'

const Chart = () => {
    return (
        <ScrollView>
            <Text>Chart Menu</Text>
        </ScrollView>
    )
}

export default Chart