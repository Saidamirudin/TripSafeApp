import React from 'react'
import { ScrollView, Text } from 'react-native'

const History = () => {
    return (
        <ScrollView>
            <Text>
                History Menu
            </Text>
        </ScrollView>
    )
}

export default History